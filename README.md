# Distracted_Driver_Detection
This project implements a deep learning-based solution to detect distracted driver behaviors from images. The model is trained on the State Farm Distracted Driver Detection dataset, which contains labeled images of drivers performing different activities such as texting, talking on the phone, adjusting the radio, eating, or driving safely.
